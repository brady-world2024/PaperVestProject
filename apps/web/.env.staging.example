# Staging web runtime configuration template.
# Use this when validating the deployed staging web app locally or in CI-like environments.

NEXT_PUBLIC_API_BASE_URL=https://api-staging.papervest.example/api
